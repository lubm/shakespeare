application: shakespy
version: 1
runtime: python27
api_version: 1
threadsafe: false

handlers:
- url: /static
  static_dir: static

- url: /mapreduce(/.*)?
  script: mapreduce.main.APP

- url: /.*
  script: main.APP

libraries:
- name: jinja2
  version: "2.6"

includes:
- mapreduce/include.yaml

